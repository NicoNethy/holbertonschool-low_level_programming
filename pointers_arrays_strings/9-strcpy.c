#include "main.h"

/**
 * _strcpy - Start point
 * @dest: destination of the code
 * @src: source of the pointer
 * Return: dest
 */

char *_strcpy(char *dest, char *src)

{
	strcpy(dest, src);
		return (dest);
}
