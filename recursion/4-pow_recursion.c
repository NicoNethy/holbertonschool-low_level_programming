#include "main.h"

/**
 * _pow_recursion - the start
 * @x: a int
 * @y: another int
 * Return: many things
 */

int _pow_recursion(int x, int y)

{
	return (x * y);
	if (y < 0)
	{
		return (-1);
	}
}
